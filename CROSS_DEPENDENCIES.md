# CROSS_DEPENDENCIES.md â€” VMS Cross-Module Dependencies

**Version**: 1.0  
**Last Updated**: 2026-06-28  
**Purpose**: Äá»‹nh nghÄ©a cÃ¡c service vÃ  middleware dÃ¹ng chÃ©o giá»¯a cÃ¡c thÃ nh viÃªn

---

## 1. Service Dependencies (Ai lÃ m gÃ¬, ai dÃ¹ng cÃ¡i gÃ¬)

### 1.1 Member 1 - CuongLH: Auth, User, Email Services

#### Services CuongLH OWNS (lÃ m vÃ  maintain)

**AuthService** (`backend/src/services/auth.service.js`)

```javascript
class AuthService {
  // Public contracts for other modules:
  async validateToken(token)              // â†’ { userId, role, email } | null
  async getUserById(userId)               // â†’ User object | null
  async verifyUserRole(userId, requiredRole)  // â†’ boolean
}
```

**EmailService** (`backend/src/services/email.service.js`)

```javascript
class EmailService {
  // Public contracts for other modules:
  async sendApplicationApproved(userEmail, eventName)
  async sendApplicationRejected(userEmail, eventName, reason)
  async sendCertificateReady(userEmail, eventName, certificateUrl)
  async sendEventReminder(userEmail, eventName, eventDate)
  async sendDonationReceipt(userEmail, amount, transactionId)
}
```

**UserService** (`backend/src/services/user.service.js`)

```javascript
class UserService {
  // Public contracts for other modules:
  async getUserById(userId)                    // â†’ User | null
  async getUsersByIds(userIds)                 // â†’ User[]
  async isUserActive(userId)                   // â†’ boolean
  async getUserRole(userId)                    // â†’ 'VOLUNTEER' | 'STAFF' | 'MANAGER' | 'ADMIN'
}
```

#### Middleware CuongLH OWNS

**authenticate** (`backend/src/middlewares/auth.middleware.js`)

```javascript
// Verify JWT cookie, inject req.user = { id, email, role }
// Usage: All protected routes MUST use this
```

**authorize(roles)** (`backend/src/middlewares/auth.middleware.js`)

```javascript
// Check if req.user.role is in allowed roles
// Usage: authorize(['STAFF', 'ADMIN'])
```

---

### 1.2 Member 2 - NamLD: Volunteer Event, Application, Feedback, Certificate Consumption

#### Services NamLD OWNS

**ApplicationService** (`backend/src/services/application.service.js`)

```javascript
class ApplicationService {
  constructor(eventService, emailService) { ... }  // DI from Member 3 and Member 1
  
  // Volunteer-facing contracts for UC12-UC14:
  async applyToEvent(userId, eventId, message)                 // â†’ Application(status=PENDING)
  async getMyApplications(userId, filters)                     // â†’ Paginated<Application>
  async cancelOwnPendingApplication(userId, applicationId, reason) // â†’ Application(status=CANCELLED)

  // Public contracts for other modules:
  async getApplicationById(applicationId)                       // â†’ Application | null
  async getApplicationByUserAndEvent(userId, eventId)           // â†’ Application | null
  async getApplicationsByEventId(eventId)                       // â†’ Application[]
  async hasUserApplied(userId, eventId)                         // â†’ boolean
  async getApprovedApplicationsCount(eventId)                   // â†’ number
}
```

**FeedbackService** (`backend/src/services/feedback.service.js`)

```javascript
class FeedbackService {
  constructor(attendanceService) { ... }        // DI from Member 3

  // Volunteer-facing contract for UC48:
  async submitFeedback(userId, eventId, comment, rating) // â†’ Feedback(status=SUBMITTED)

  // Public contracts for other modules:
  async getFeedbacksByEventId(eventId)         // â†’ Feedback[]
  async getAverageRating(eventId)              // â†’ number (1-5)
}
```

#### NamLD DEPENDS ON

- **Auth/UserService** (Member 1): To provide `currentUser.id`, `currentUser.role`, and `currentUser.isActive` for UC12, UC13, UC14, UC48, UC51, UC52.
- **EventService** (Member 3): To consume public event list/detail for UC08-UC11 and validate event availability, capacity, and deadline for UC12.
- **AttendanceService** (Member 3): To verify successful attendance by `userId + eventId` before UC48 feedback submission.
- **CertificateService** (Member 3): To list, view, and download certificates generated by Staff for UC51-UC52.
- **CategoryService** (Member 4): To consume active category summary for Event List/Search/Filter when not embedded in `GET /events`.
- **SkillService** (Member 4): To consume active skill summary and event-skill filters for UC09-UC11 when not embedded in `GET /events`.
- **OrganizationService** (Member 5): To consume active organization summary for Event List/Search/Filter when not embedded in `GET /events`.
- **EmailService** (Member 1) and **NotificationService** (Member 5): Optional side effects after application/certificate status changes; they MUST NOT replace the main database transaction.

#### NamLD Canonical Status Contracts

- Event status: `DRAFT`, `PUBLISHED`, `IN_PROGRESS`, `COMPLETED`, `CANCELLED`.
- Public/discoverable event: `status = PUBLISHED AND isActive = true`.
- Application status: `PENDING`, `APPROVED`, `REJECTED`, `CANCELLED`.
- Certificate status: `GENERATING`, `AVAILABLE`, `REVOKED`, `UNAVAILABLE`.

---

### 1.3 Member 3 - TienTD: Event, Attendance, Certificate Services

#### Services TienTD OWNS

**EventService** (`backend/src/services/event.service.js`)

```javascript
class EventService {
  // Public contracts for other modules:
  async getEventById(eventId)                  // â†’ Event | null
  async isEventActive(eventId)                 // â†’ boolean
  async getPublicEvents(filters)               // â†’ Paginated<EventSummary>; status=PUBLISHED AND isActive=true
  async getPublicEventDetail(eventId)          // â†’ EventDetail | null; public/discoverable only
  async canAcceptApplication(eventId)          // â†’ { canApply, reason, remainingSlots }
  async getEventCapacity(eventId)              // â†’ { max_capacity, current_participants }
  async incrementParticipantCount(eventId)     // â†’ void (for approved applications)
  async decrementParticipantCount(eventId)     // â†’ void (only if an approved participation is removed by Staff)
  async getEventsByOrganizationId(orgId)       // â†’ Event[]
}
```

**AttendanceService** (`backend/src/services/attendance.service.js`)

```javascript
class AttendanceService {
  constructor(applicationService) { ... }      // DI tá»« Member 2
  
  // Public contracts for other modules:
  async hasAttended(applicationId)             // â†’ boolean
  async hasSuccessfulAttendance(userId, eventId) // â†’ boolean; for UC48 feedback eligibility
  async getAttendanceByApplicationId(appId)    // â†’ Attendance | null
}
```

**CertificateService** (`backend/src/services/certificate.service.js`)

```javascript
class CertificateService {
  constructor(attendanceService, applicationService, emailService) { ... }
  
  // Public contracts for other modules:
  async getCertificatesByUserId(userId, filters) // â†’ Paginated<Certificate>
  async getCertificateForOwner(certificateId, userId) // â†’ Certificate | null
  async canDownloadCertificate(certificateId, userId) // â†’ { canDownload, reason }
}
```

#### TienTD DEPENDS ON

- **ApplicationService** (Member 2): Äá»ƒ verify application status trÆ°á»›c khi check-in
- **AttendanceService** (own): Äá»ƒ verify attendance trÆ°á»›c khi generate certificate
- **EmailService** (Member 1): Äá»ƒ gá»­i email khi certificate ready

---

### 1.4 Member 4 - AnhND: UserManagement, Category, Skill Services

#### Services AnhND OWNS

**UserManagementService** (`backend/src/services/user-management.service.js`)

```javascript
class UserManagementService {
  // Public contracts for other modules:
  async getAllUsers(filters, pagination)       // â†’ { users[], total }
  async updateUserRole(userId, newRole)        // â†’ User
  async deactivateUser(userId)                 // â†’ void
}
```

**CategoryService** (`backend/src/services/category.service.js`)

```javascript
class CategoryService {
  // Public contracts for other modules:
  async getAllCategories()                     // â†’ Category[]
  async getActiveCategorySummaries()           // â†’ CategorySummary[] for public filters
  async getCategoryById(categoryId)            // â†’ Category | null
  async isCategoryActive(categoryId)           // â†’ boolean
}
```

**SkillService** (`backend/src/services/skill.service.js`)

```javascript
class SkillService {
  // Public contracts for other modules:
  async getAllSkills()                         // â†’ Skill[]
  async getActiveSkillSummaries()              // â†’ SkillSummary[] for public filters
  async getSkillById(skillId)                  // â†’ Skill | null
  async isSkillActive(skillId)                 // â†’ boolean
}
```

#### AnhND DEPENDS ON

- **EventService** (Member 3): Äá»ƒ aggregate event statistics cho admin dashboard
- **ApplicationService** (Member 2): Äá»ƒ aggregate application statistics

---

### 1.5 Member 5 - DucNM: Organization, Notification, Dashboard, Payment Services

#### Services DucNM OWNS

**OrganizationService** (`backend/src/services/organization.service.js`)

```javascript
class OrganizationService {
  // Public contracts for other modules:
  async getOrganizationById(orgId)             // â†’ Organization | null
  async isOrganizationActive(orgId)            // â†’ boolean
  async getAllOrganizations()                  // â†’ Organization[]
  async getActiveOrganizationSummaries()       // â†’ OrganizationSummary[] for public filters
}
```

**NotificationService** (`backend/src/services/notification.service.js`)

```javascript
class NotificationService {
  constructor(emailService) { ... }            // DI tá»« Member 1
  
  // Public contracts for other modules:
  async createNotification(userId, type, message, metadata)  // â†’ Notification
  async sendApplicationNotification(userId, applicationId, status)
  async sendEventReminder(userId, eventId)
  async sendCertificateNotification(userId, certificateId)
}
```

**DashboardService** (`backend/src/services/dashboard.service.js`)

```javascript
class DashboardService {
  constructor(eventService, applicationService, userService) { ... }
  
  // Public contracts for other modules:
  async getVolunteerStats(userId)              // â†’ { total_events, total_hours, certificates }
  async getStaffStats(staffId)                 // â†’ { created_events, total_participants }
  async getAdminStats()                        // â†’ { total_users, total_events, total_donations }
}
```

**PaymentService** (`backend/src/services/payment.service.js`)

```javascript
class PaymentService {
  // Public contracts for other modules:
  async createPaymentIntent(userId, amount, metadata)  // â†’ { payment_id, payment_url }
  async verifyPaymentCallback(paymentData)             // â†’ { success, transaction_id }
}
```

#### DucNM DEPENDS ON

- **EmailService** (Member 1): Äá»ƒ gá»­i donation receipt
- **EventService** (Member 3): Äá»ƒ aggregate event stats
- **ApplicationService** (Member 2): Äá»ƒ aggregate application stats
- **UserService** (Member 1): Äá»ƒ get user info cho dashboard

---

## 2. Middleware Dependencies (Ai dÃ¹ng middleware nÃ o)

### 2.1 Authentication Middleware (Member 1)

**File**: `backend/src/middlewares/auth.middleware.js`

**Owner**: Member 1 - CuongLH

**Functions**:

```javascript
export const authenticate = async (req, res, next) => {
  // Verify JWT from cookie, inject req.user = { id, email, role }
}

export const authorize = (roles) => async (req, res, next) => {
  // Check if req.user.role is in allowed roles array
}
```

**Ai pháº£i dÃ¹ng**:

| Member | Routes cáº§n authenticate | Routes cáº§n authorize |
|--------|------------------------|---------------------|
| Member 1 | `/profile/*`, `/auth/logout` | - |
| Member 2 | `/events/:eventId/applications`, `/me/applications`, `/applications/:applicationId/cancel`, `/events/:eventId/feedback`, `/me/certificates`, `/certificates/:certificateId`, `/certificates/:certificateId/download` | authorize(['VOLUNTEER']) |
| Member 3 | `/staff/events/*`, `/staff/applications/*`, `/attendance/*` | authorize(['STAFF', 'MANAGER']) |
| Member 4 | `/admin/users/*`, `/admin/categories/*`, `/admin/skills/*` | authorize(['ADMIN', 'MANAGER']) |
| Member 5 | `/organizations/*`, `/donations/*`, `/dashboard/*` | Mixed (depends on route) |

---

### 2.2 Validation Middleware (Shared)

**Pattern**: Má»—i member tá»± táº¡o Zod schemas cho module cá»§a mÃ¬nh

**Shared Utility**: `backend/src/middlewares/validate.middleware.js`

```javascript
export const validate = (schema) => async (req, res, next) => {
  try {
    req.body = await schema.parseAsync(req.body);
    next();
  } catch (error) {
    return res.status(400).json({ success: false, error: error.message });
  }
}
```

**Usage Example**:

```javascript
// Member 2 route
router.post('/applications', 
  authenticate, 
  validate(applicationSchema),  // Own schema
  applicationController.create
);
```
**Member 2 Event Discovery note**: For Member 2 Event Discovery query validation, invalid query params should be mapped to `422` according to `API_CONTRACTS.md`. This can be handled in event-specific validator/controller without changing unrelated modules.

---

### 2.3 Error Handling Middleware (Shared)

**File**: `backend/src/middlewares/error.middleware.js`

**Owner**: Shared (member nÃ o setup backend Ä‘áº§u tiÃªn)

```javascript
export const errorHandler = (err, req, res, next) => {
  logger.error(err);
  res.status(err.statusCode || 500).json({
    success: false,
    error: err.message || 'Internal Server Error'
  });
}
```

**Ai pháº£i dÃ¹ng**: Táº¥t cáº£ members (global middleware)

---

## 3. Cross-Module Call Flow Examples

### Example 1: Approve Application (Member 3 calls Member 2, Member 1)

```javascript
// backend/src/services/application.service.js (Member 2 + Member 3)
class ApplicationService {
  constructor(eventService, emailService) {
    this.eventService = eventService;      // From Member 3
    this.emailService = emailService;      // From Member 1
  }

  async approveApplication(applicationId, reviewerId) {
    const application = await this.repository.findById(applicationId);
    
    // Call Member 3's EventService
    const capacity = await this.eventService.getEventCapacity(application.event_id);
    if (capacity.current_participants >= capacity.max_capacity) {
      throw new Error('Event capacity full');
    }
    
    // Update application
    await this.repository.updateStatus(applicationId, 'APPROVED');
    await this.eventService.incrementParticipantCount(application.event_id);
    
    // Call Member 1's EmailService
    const user = await this.userService.getUserById(application.user_id);
    await this.emailService.sendApplicationApproved(user.email, application.event.name);
    
    return application;
  }
}
```

---

### Example 2: Generate Certificate (Member 3 calls Member 2, Member 1, Member 5)

```javascript
// backend/src/services/certificate.service.js (Member 3)
class CertificateService {
  constructor(attendanceService, applicationService, emailService, notificationService) {
    this.attendanceService = attendanceService;    // From Member 3 (own)
    this.applicationService = applicationService;  // From Member 2
    this.emailService = emailService;              // From Member 1
    this.notificationService = notificationService; // From Member 5
  }

  async generateCertificate(applicationId) {
    // Verify attendance (own service)
    const hasAttended = await this.attendanceService.hasAttended(applicationId);
    if (!hasAttended) {
      throw new Error('User did not attend event');
    }
    
    // Get application details (Member 2)
    const application = await this.applicationService.getApplicationById(applicationId);
    
    // Generate certificate URL (Cloudinary logic here)
    const certificateUrl = await this.cloudinaryService.generate(...);
    
    // Save to database
    const certificate = await this.repository.create({
      application_id: applicationId,
      certificate_url: certificateUrl
    });
    
    // Notify user (Member 5)
    await this.notificationService.sendCertificateNotification(
      application.user_id, 
      certificate.id
    );
    
    // Send email (Member 1)
    await this.emailService.sendCertificateReady(
      application.user.email,
      application.event.name,
      certificateUrl
    );
    
    return certificate;
  }
}
```

---

### Example 3: Dashboard Stats (Member 5 calls Member 1, Member 2, Member 3)

```javascript
// backend/src/services/dashboard.service.js (Member 5)
class DashboardService {
  constructor(eventService, applicationService, userService) {
    this.eventService = eventService;          // From Member 3
    this.applicationService = applicationService; // From Member 2
    this.userService = userService;            // From Member 1
  }

  async getVolunteerStats(userId) {
    // Verify user exists (Member 1)
    const user = await this.userService.getUserById(userId);
    if (!user) throw new Error('User not found');
    
    // Get applications (Member 2)
    const applications = await this.applicationService.getApplicationsByUserId(userId);
    const approvedCount = applications.filter(app => app.status === 'APPROVED').length;
    
    // Get attended events (Member 3)
    const attendedEvents = await this.eventService.getAttendedEventsByUserId(userId);
    
    return {
      total_applications: applications.length,
      approved_applications: approvedCount,
      total_events_attended: attendedEvents.length,
      total_volunteer_hours: attendedEvents.reduce((sum, e) => sum + e.hours, 0)
    };
  }
}
```

---

## 4. Dependency Injection Setup (App-level)

**File**: `backend/src/app.js` hoáº·c `backend/src/server.js`

```javascript
import AuthService from './services/auth.service.js';
import EmailService from './services/email.service.js';
import UserService from './services/user.service.js';
import EventService from './services/event.service.js';
import ApplicationService from './services/application.service.js';
import AttendanceService from './services/attendance.service.js';
import CertificateService from './services/certificate.service.js';
import NotificationService from './services/notification.service.js';
import DashboardService from './services/dashboard.service.js';

// Repositories
import AuthRepository from './repositories/auth.repository.js';
import EventRepository from './repositories/event.repository.js';
// ... other repositories

// Initialize services with dependencies
const authRepository = new AuthRepository();
const authService = new AuthService(authRepository);

const emailService = new EmailService();
const userService = new UserService(userRepository);

const eventRepository = new EventRepository();
const eventService = new EventService(eventRepository);

const applicationRepository = new ApplicationRepository();
const applicationService = new ApplicationService(
  applicationRepository,
  eventService,        // Cross-module dependency
  emailService         // Cross-module dependency
);

const attendanceRepository = new AttendanceRepository();
const attendanceService = new AttendanceService(
  attendanceRepository,
  applicationService   // Cross-module dependency
);

const certificateRepository = new CertificateRepository();
const certificateService = new CertificateService(
  certificateRepository,
  attendanceService,   // Own module
  applicationService,  // Cross-module
  emailService,        // Cross-module
  notificationService  // Cross-module
);

const notificationRepository = new NotificationRepository();
const notificationService = new NotificationService(
  notificationRepository,
  emailService         // Cross-module dependency
);

const dashboardService = new DashboardService(
  eventService,        // Cross-module
  applicationService,  // Cross-module
  userService          // Cross-module
);

// Export for controllers
export {
  authService,
  emailService,
  userService,
  eventService,
  applicationService,
  attendanceService,
  certificateService,
  notificationService,
  dashboardService
};
```

---

## 5. Quick Reference Table

### Service Dependencies Matrix

| Service | Owner | Depends On (Cross-Module) |
|---------|-------|---------------------------|
| **AuthService** | Member 1 | - |
| **EmailService** | Member 1 | - |
| **UserService** | Member 1 | - |
| **ApplicationService** | Member 2 | Auth/UserService (M1), EventService (M3), EmailService (M1), NotificationService (M5 optional) |
| **FeedbackService** | Member 2 | AttendanceService (M3) |
| **EventService** | Member 3 | - |
| **AttendanceService** | Member 3 | ApplicationService (M2) |
| **CertificateService** | Member 3 | AttendanceService (own), ApplicationService (M2), EmailService (M1), NotificationService (M5) |
| **UserManagementService** | Member 4 | EventService (M3), ApplicationService (M2) |
| **CategoryService** | Member 4 | EventService/NamLD consumers use active summaries |
| **SkillService** | Member 4 | EventService/NamLD consumers use active summaries |
| **OrganizationService** | Member 5 | EventService/NamLD consumers use active summaries |
| **NotificationService** | Member 5 | EmailService (M1) |
| **DashboardService** | Member 5 | EventService (M3), ApplicationService (M2), UserService (M1) |
| **PaymentService** | Member 5 | EmailService (M1) |

### Middleware Usage Matrix

| Middleware | Owner | Used By All? | Notes |
|------------|-------|--------------|-------|
| `authenticate` | Member 1 | âœ… Yes | All protected routes |
| `authorize(roles)` | Member 1 | âœ… Yes | Role-based access control |
| `validate(schema)` | Shared | âœ… Yes | Each member provides own schemas |
| `errorHandler` | Shared | âœ… Yes | Global error handler |
| `logger` | Shared | âœ… Yes | Request/response logging |

---

## 6. Rules for Cross-Module Communication

### âœ… DO

1. **Use Dependency Injection**: Inject dependencies via constructor
2. **Call Service methods**: Always use public service contracts
3. **Handle errors**: Wrap cross-module calls in try-catch
4. **Document contracts**: Update this file when adding new public methods
5. **Version APIs**: Use semantic versioning for breaking changes

### âŒ DON'T

1. **Direct Repository calls**: NEVER import another module's repository
2. **HTTP fetch()**: NEVER use fetch/axios for internal backend-to-backend calls
3. **Tight coupling**: Don't expose internal implementation details
4. **Circular dependencies**: Design to avoid circular service dependencies
5. **Bypass middleware**: Don't skip authentication/authorization for cross-module calls

---

**Version History**:

- v1.0 (2026-06-28): Initial version - Cross-module dependencies for 5 members
